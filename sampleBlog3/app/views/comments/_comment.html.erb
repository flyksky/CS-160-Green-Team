<p><%= comment.author %> said:</p>

<blockquote>
  <%= comment.body %>
</blockquote>

<p>
<%= link_to 'Edit', edit_post_comment_path(comment.post, comment) %>
  
  <%= button_to 'Destroy', [comment.post, comment],
 method: :delete, data: { confirm: 'Are you sure?' } %>
</p>